define_attribute CatenaDesignType -data_type string -obj_type base_cell -category lef_property -help_string {This was created by a LEF PROPERTYDEFINITION statement with the name "CatenaDesignType". Any change to the value is only valid during the current session. The next read_db will recreate these attribute names and values from the LEF file only.} -default_value {deviceLevel} -quiet
